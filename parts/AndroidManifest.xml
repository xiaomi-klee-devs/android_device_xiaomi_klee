<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2023 Paranoid Android

     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xiaomi.settings"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_SURFACE_FLINGER" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:label="@string/app_name"
        android:persistent="true"
        android:defaultToDeviceProtectedStorage="true"
        android:directBootAware="true">

        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".display.ColorService"
            android:exported="false">
        </service>

        <!-- Services -->
        <service android:name=".battery.ChargingLimitService" />

        <!-- Battery/Charging Activity -->
        <activity
            android:name=".battery.ChargingControlActivity"
            android:label="@string/charging_control_title"
            android:exported="true">
            <intent-filter android:priority="1">
                <action android:name="com.android.settings.action.IA_SETTINGS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.battery" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/charging_control_summary" />
        </activity>

        <!-- ====================================================== -->
        <!-- Special Features Hub — The only one entry on Settings  -->
        <!-- ====================================================== -->
        <activity
            android:name=".hub.SpecialFeaturesActivity"
            android:label="@string/special_features_title"
            android:icon="@drawable/ic_xiaomiparts"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.system" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_xiaomiparts" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/special_features_summary" />
        </activity>

        <!-- ====================================================== -->
        <!-- GameBar — Get called by SpecialFeaturesFragment        -->
        <!-- ====================================================== -->
        <activity
            android:name=".gamebar.GameBarSettingsActivity"
            android:label="@string/game_bar_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <!-- GameBar AppSelector -->
        <activity android:name=".gamebar.GameBarAppSelectorActivity" />
        <activity android:name=".gamebar.GameBarAppRemoverActivity" />

        <!-- GameBar Tile Service -->
        <service
            android:name=".gamebar.GameBarTileService"
            android:label="@string/game_bar_tile_label"
            android:icon="@drawable/ic_gamebar"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- GameBar Monitor Service -->
        <service
            android:name=".gamebar.GameBarMonitorService"
            android:exported="false" />

        <!-- GameBar BootReceiver -->
        <receiver
            android:name=".gamebar.GameBarBootReceiver"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- ====================================================== -->
        <!-- Touch Sampling — Get called by SpecialFeaturesFragment -->
        <!-- ====================================================== -->
        <activity
            android:name=".touchsampling.TouchSamplingSettingsActivity"
            android:label="@string/htsr_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <service
            android:name=".touchsampling.TouchSamplingService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <activity android:name=".touchsampling.TouchSamplingAppSelectorActivity" />

        <!-- Touch Sampling Tile Service -->
        <service
            android:name=".touchsampling.TouchSamplingTileService"
            android:label="@string/touch_sampling_tile_label"
            android:icon="@drawable/ic_touch_sampling_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- ====================================================== -->
        <!-- Core Control — Get called by SpecialFeaturesFragment   -->
        <!-- ====================================================== -->
        <activity
            android:name=".corecontrol.CoreControlActivity"
            android:label="@string/core_control_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true" />

        <!-- ====================================================== -->
        <!-- Backlight — Get called by SpecialFeaturesFragment      -->
        <!-- ====================================================== -->
        <activity
            android:name=".light.LightSettingsActivity"
            android:label="@string/light_settings_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <activity android:name=".light.AppSelectorActivity" />

        <service
            android:name=".light.LightService"
            android:exported="false" />

        <service
            android:name=".light.LightNotificationService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
